<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#aa4f43"
                android:centerColor="#aa4f43"
                android:centerY="1.0"
                android:endColor="#aa4f43"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#1420af"
                    android:centerColor="#1420af"
                    android:centerY="1.0"
                    android:endColor="#1420af"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>